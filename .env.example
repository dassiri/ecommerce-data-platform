# PostgreSQL connection configuration (Step 3.2.4 / DEC list).
# Copy this file to .env and fill in real values locally. Do not commit .env.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ecommerce_dw
POSTGRES_USER=
POSTGRES_PASSWORD=
